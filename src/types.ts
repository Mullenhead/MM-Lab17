export type Slide = { 
        id: number, 
        order: number, 
        image: string, 
        description: string,
        text: string, 
        fontColor: string,
        speakerNotes: string 
    }

  